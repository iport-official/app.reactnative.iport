import styled from 'styled-components/native'

import { colors } from '../../styles'

export const ProfileImage = styled.Image`
    width: 50px;
    height: 50px;
    border-radius: 25px;
    border: 2px solid ${colors.ligherPurple};
`
