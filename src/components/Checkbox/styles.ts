import styled from 'styled-components/native';
import { colors } from '../../styles';

export const CheckboxStyle = styled.View`
    border-width: 1px;
    border-color: ${colors.grayPurple}aa;
    border-style: solid;
    border-radius: 20px;

    background-color: ${colors.lightPurple}70;
    overflow: hidden;

    height: 22px;
    width: 22px;
`
